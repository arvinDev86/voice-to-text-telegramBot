# 🎙️ Telegram Voice to Text Bot (Gemini AI + Fallback)

ربات تلگرام برای تبدیل ویس و فایل‌های صوتی به متن با استفاده از هوش مصنوعی **Google Gemini 1.5** و پشتیبانی از روش جایگزین (Fallback) در صورت قطعی.

این ربات به طور خاص برای زبان **فارسی** بهینه شده و قابلیت تشخیص محاوره‌ها، لهجه‌ها و اصلاح جملات را دارد.

## ✨ ویژگی‌ها

- **دقت بسیار بالا:** استفاده از مدل `Gemini 2.5 Flash` برای درک عمیق جملات و تبدیل دقیق.
- **سیستم جایگزین (Fallback):** اگر به هر دلیلی (اینترنت، تمام شدن اعتبار API و...) هوش مصنوعی کار نکند، ربات به صورت خودکار و بدون توقف روی موتور `Google Speech Recognition` سوییچ می‌کند.
- **پشتیبانی از فرمت‌ها:** تبدیل خودکار فرمت `OGG` (ویس تلگرام) به `MP3/WAV` برای پردازش.
- **سرعت بالا:** استفاده از مدل Flash برای پاسخگویی سریع.

## 🛠 پیش‌نیازها

قبل از اجرا، مطمئن شوید که موارد زیر را دارید:

1. **Python 3.8** یا بالاتر.
2. **FFmpeg:** برای پردازش فایل‌های صوتی .
3. **Telegram Bot Token:** از ربات [@BotFather](https://t.me/BotFather) بگیرید.
4. **Gemini API Key:** از [Google AI Studio](https://aistudio.google.com/) بگیرید.

## 🚀 نصب و راه‌اندازی

### ۱. دریافت پروژه
ترمینال را باز کنید و مخزن را کلون کنید (یا فایل‌ها را دانلود کنید):
```bash
git clone https://github.com/arvinDev86/voice-to-text-telegramBot.git

cd voice-to-text-telegramBot
